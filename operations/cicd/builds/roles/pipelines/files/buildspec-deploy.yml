version: 0.2
env:
  shell: bash
phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - pip install -r operations/requirements.txt
  build:
    commands:
      - printenv
      - python --version
      - python3 --version
      - pip --version
      - which python
      - which python3
      - ansible-playbook --version
      - which ansible-playbook
      - cat `which ansible-playbook` | head -n 5
      - cd operations
      - ansible-playbook -vvv -i inventories/dev/hosts apps/deploy/app-deployment.yml -e 'ansible_python_interpreter=/usr/bin/python3'
